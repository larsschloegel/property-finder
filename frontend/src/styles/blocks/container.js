import {css} from "styled-components";

export const containerStyles = css(
    ({theme}) => css`
      .container{
        width: 100%;
        max-width: 1280px;
        margin: 0 auto;
        padding: 0 80px;
      }
        
        
    `
)